# Kotaschool Frontend

Copiez `.env.local.example` vers `.env.local`, installez les dépendances avec `npm install`, puis lancez `npm run dev`.

L'application est exposée sur `http://localhost:3000` et utilise l'API sur le port 4000 par défaut.
