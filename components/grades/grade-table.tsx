'use client';
export type GradeRow = { idInscription: string; matricule: string; nom: string; postnom?: string | null; prenom: string; valeurNote: number | null; observation: string; estValide: boolean };
export function GradeTable({ rows, maximum, onChange, readOnly = false }: { rows: GradeRow[]; maximum: number; onChange: (rows: GradeRow[]) => void; readOnly?: boolean }) {
  function update(index: number, patch: Partial<GradeRow>) { onChange(rows.map((row, i) => i === index ? { ...row, ...patch } : row)); }
  return <div className="overflow-x-auto rounded-lg bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-slate-100 text-slate-600"><tr><th className="p-3">Matricule</th><th className="p-3">Élève</th><th className="p-3">Note / {maximum}</th><th className="p-3">Observation</th><th className="p-3">Statut</th></tr></thead><tbody>{rows.map((row, index) => <tr key={row.idInscription} className="border-t"><td className="p-3 font-mono text-xs">{row.matricule}</td><td className="p-3">{row.nom} {row.postnom} {row.prenom}</td><td className="p-3"><input type="number" disabled={readOnly} min="0" max={maximum} value={row.valeurNote ?? ''} onChange={(e) => { const value = e.target.value === '' ? null : Math.min(maximum, Math.max(0, Number(e.target.value))); update(index, { valeurNote: value }); }} className="w-24 rounded border p-2" aria-label={`Note de ${row.nom}`} /></td><td className="p-3"><input disabled={readOnly} value={row.observation} onChange={(e) => update(index, { observation: e.target.value })} className="w-full rounded border p-2" placeholder="Facultatif" /></td><td className="p-3"><span className={`rounded-full px-2 py-1 text-xs font-medium ${row.estValide ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700'}`}>{row.estValide ? 'Validée' : 'Brouillon'}</span></td></tr>)}</tbody></table></div>;
}
